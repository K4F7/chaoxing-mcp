# 学习通课程作业

给个人使用的学习通课程作业查询能力。Agent 通过它读取当前受监控课程里的待办事项；登录态只存在于运行查询的那台机器上。

## Language

### 待办与来源

**待办事项**：
一件有截止时间、需要用户完成的学习任务，包括作业、考试和测验。
_Avoid_：任务、item、todo

**来源**：
一件待办事项被发现的途径。同一件待办事项可以从多个来源被发现，但对调用方只呈现一次。
_Avoid_：数据源、渠道

**受监控课程**：
用户明确选定、每次查询都要检查的课程。未被选中的课程不参与扫描，因此它们的待办事项只能通过其他来源被发现。
_Avoid_：关注课程、订阅课程

### 失效与信任

**静默失效**：
工具表面上在正常工作、调用方看不到任何异常，实际却已经漏掉了本该出现的待办事项。它比明显报错更危险，因为调用方会把空结果当成「没有作业」。取舍原则是：宁可犯可见的错，不可犯静默的漏。
_Avoid_：静默错误、无声失败

**认证失效**：
用户的学习通登录态不再有效，工具无法再代表用户读取数据。这一状态必须对调用方可见，因为它会让查询停在原地却不产生任何新信息，是静默失效最常见的成因。
_Avoid_：Cookie 过期、掉线、未登录

**URL 信任分级**：
按照「能否携带用户凭据访问」对目标地址划分的等级。学习通之外的地址、非加密地址和畸形地址都不属于可信级别，工具不会带着凭据请求它们。
_Avoid_：白名单、域名校验
